{
  "name": "@aic/example-vite-crm-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aic/devtools": "workspace:*",
    "@aic/integrations-shadcn": "workspace:*",
    "@aic/sdk-react": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "vite": "^6.2.6"
  }
}
